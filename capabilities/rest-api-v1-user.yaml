naftiko: 1.0.0-alpha2
info:
  label: CircleCI REST API v1 — User
  description: 'CircleCI REST API v1 — User. 1 operations. Lead operation: Get authenticated user. Self-contained Naftiko
    capability covering one Circleci business surface.'
  tags:
  - Circleci
  - User
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CIRCLECI_API_KEY: CIRCLECI_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-v1-user
    baseUri: https://circleci.com/api/v1.1
    description: CircleCI REST API v1 — User business capability. Self-contained, no shared references.
    resources:
    - name: me
      path: /me
      operations:
      - name: getcurrentuser
        method: GET
        description: Get authenticated user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Circle-Token
      value: '{{env.CIRCLECI_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rest-api-v1-user-rest
    port: 8080
    description: REST adapter for CircleCI REST API v1 — User. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/me
      name: me
      description: REST surface for me.
      operations:
      - method: GET
        name: getcurrentuser
        description: Get authenticated user
        call: rest-api-v1-user.getcurrentuser
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-v1-user-mcp
    port: 9090
    transport: http
    description: MCP adapter for CircleCI REST API v1 — User. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-authenticated-user
      description: Get authenticated user
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-v1-user.getcurrentuser
      outputParameters:
      - type: object
        mapping: $.
