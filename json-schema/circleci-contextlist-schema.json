{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ContextList",
  "title": "ContextList",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/Context"
      },
      "description": "List of contexts"
    },
    "next_page_token": {
      "type": "string",
      "description": "Token for retrieving the next page"
    }
  }
}